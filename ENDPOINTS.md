# Endpoints PUT e DELETE

## PUT /tarefas/{id}
Atualiza uma tarefa existente.
Retorna 200 se atualizado ou 404 se não encontrado.

## DELETE /tarefas/{id}
Remove uma tarefa existente.
Retorna 200 se removido ou 404 se não encontrado.

